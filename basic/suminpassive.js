function printSumInPassiveTense(sum) {
    // Use template literals to construct the passive tense sentence
    const sentence = `${sum} was calculated as the sum.`;
    
    // Print the passive tense sentence
    console.log(sentence);
  }
  
  // Example usage
  const totalSum = 100;
  printSumInPassiveTense(totalSum);
  